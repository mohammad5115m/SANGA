[pytest]
DJANGO_SETTINGS_MODULE = config.settings.development
python_files = tests.py test_*.py *_tests.py
addopts = -q
markers =
    concurrency: needs a real concurrent database; runs only on the PostgreSQL lane
